#include<stdio.h>
int main()
{
    int n,j,i;
    scanf("%d",&n);
    int element[n];
    for (j=0;j<n;j++)
    {
        scanf("%d",&element[j]);
    }
    for(i=n-1;i>=0;i--)
    {
        printf("%d ",element[i]);
    }
}
