#include<stdio.h>
int main(){
    int number,count=0;
    scanf("%d",&number);
    do {
        number/=10;
        count++ ;
    }
    while(number !=0);
    printf("%d",count);
    return 0;
        
    }
