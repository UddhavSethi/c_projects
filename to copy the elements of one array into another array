#include<stdio.h>
int main()
{
    int n,j,i;
    scanf("%d",&n);
    int element[n];
    for (j=0;j<n;j++)
    {
        scanf("%d",&element[j]);
    }
    for(i=0;i<n;i++)
    {
     printf("%d ",element[i]);
    }
    return 0;
}
